# Disable Spring Cloud Config Server for local development
# The local profile uses application-local.properties directly
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false
