{
  "skipStatusChecks": {{skipStatusChecks}},
  "statusCheckFilters": [
    {{#each statusCheckFilters}}
      "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "includeClaims": {{includeClaims}},
  "transactionId": "{{transactionId}}"
}